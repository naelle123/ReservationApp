name: meeting_room_booking
description: Application professionnelle de réservation de salles de réunion
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Material Design
  cupertino_icons: ^1.0.6
  material_color_utilities: ^0.5.0
  
  # HTTP & API
  http: ^1.1.0
  dio: ^5.3.2
  
  # State Management
  provider: ^6.1.1
  
  # Storage & Persistence
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  
  # Date & Time
  intl: ^0.18.1
  table_calendar: ^3.0.9
  
  # Navigation
  go_router: ^12.1.1
  
  # Utils & Logging
  logger: ^2.0.2+1
  
  # UI Components & Animations
  flutter_spinkit: ^5.2.0
  fluttertoast: ^8.2.4
  shimmer: ^3.0.0
  lottie: ^2.7.0
  
  # Icons & Fonts (via packages, pas d'assets locaux)
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Pas d'assets locaux - tout via des packages ou des URLs